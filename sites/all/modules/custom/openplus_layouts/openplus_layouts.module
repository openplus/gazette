<?php
/**
 * @file
 * openplus_layouts.module
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function openplus_layouts_ctools_plugin_directory($module, $plugin) {
  if ($module == 'panels' && $plugin == 'layouts') {
    return 'plugins/layouts';
  }
}
